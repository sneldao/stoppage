# SigNoz admin credentials for scripts/provision-signoz.ts (mode 600, gitignored).
# Created during VPS install — copy values from your SigNoz setup.
SIGNOZ_URL=http://127.0.0.1:9090
SIGNOZ_EMAIL=ops@stoppage.local
SIGNOZ_PASSWORD=replace-me
SIGNOZ_ORG_ID=replace-me
