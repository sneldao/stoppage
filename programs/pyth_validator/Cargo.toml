[package]
name = "pyth_validator"
version = "0.1.0"
description = "Stoppage price validator: verifies a Pyth PriceUpdateV2 observation against a threshold and returns a bool over CPI"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "pyth_validator"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build"]
# Silence `unexpected_cfgs` warnings from Anchor macro internals.
custom-heap = []
custom-panic = []
anchor-debug = []

[dependencies]
anchor-lang = { version = "0.32.1", features = ["init-if-needed"] }
